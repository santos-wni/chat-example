config:
  target: "http://localhost:3000"
  phases:
    - duration: 10
      arrivalRate: 2
scenarios:
  - engine: "socketio"
    flow:
      # - emit:
      #     channel: "room"
      #     data: "Hello"
      - think: 5