# Run: artillery run test.yaml
config:
  target: "http://localhost:3000"
  phases:
    - duration: 1
      arrivalRate: 100
scenarios:
  - engine: "socketio"
    flow:
      # - emit:
      #     channel: "room"
      #     data: "Hello"
      - think: 60